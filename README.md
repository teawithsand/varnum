# varnum
Bignums implementation in rust.
Allows creating numbers with specified size, for instance 4096 bytes.
Works with no-std and without heap allocation(when constant size is used).

# WIP
Work in progress.